<!-- /snippets/popup-address.liquid -->

{% case popup-address %}
  {% when 'edit' %}

    <div id="PopupAddress_{{ address.id }}" class="AddressPopup popup overlay">
      <div class="popup-inner">
        <div class="popup-content">
          {% form 'customer_address', address %}
            <h4 class="AccountHeading AccountHeading--popup">{{ 'account.address.edit' | t }}</h4>
            <hr>

            {{ form.errors | default_errors }}
            <div class="AddressPopup__two-column">
              <label class="hidden" for="AddressFirstName_{{ address.id }}">{{ 'account.customer.first_name' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.first_name' | t }}" id="AddressFirstName_{{ address.id }}" name="address[first_name]" value="{{ form.first_name }}" autocapitalize="words">

              <label class="hidden" for="AddressLastName_{{ address.id }}">{{ 'account.customer.last_name' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.last_name' | t }}" id="AddressLastName_{{ address.id }}" name="address[last_name]" value="{{ form.last_name }}" autocapitalize="words">
            </div>

            <div>
              <label class="hidden" for="AddressCompany_{{ address.id }}">{{ 'account.customer.company' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.company' | t }}" id="AddressCompany_{{ address.id }}" name="address[company]" value="{{ address.company }}" autocapitalize="words">
            </div>

            <div>
              <label class="hidden" for="AddressAddress1_{{ address.id }}">{{ 'account.customer.address1' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.address1' | t }}" id="AddressAddress1_{{ address.id }}" name="address[address1]" value="{{ address.address1 }}" autocapitalize="words">
            </div>

            <div>
              <label class="hidden" for="AddressAddress2_{{ address.id }}">{{ 'account.customer.address2' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.address2' | t }}" id="AddressAddress2_{{ address.id }}" name="address[address2]" value="{{ address.address2 }}" autocapitalize="words">
            </div>

            <div class="AddressPopup__two-column">
              <label class="hidden" for="AddressCity_{{ address.id }}">{{ 'account.customer.city' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.city' | t }}" id="AddressCity_{{ address.id }}" name="address[city]" value="{{ address.city }}" autocapitalize="words">

              <label class="hidden" for="AddressCountry_{{ address.id }}">{{ 'account.customer.country' | t }}</label>
              <div class="hidden select-wrapper"><select id="AddressCountry_{{ address.id }}" name="address[country]" data-default="{{ form.country }}">{{ country_option_tags }}</select></div>

              <div id="AddressProvinceContainer_{{ address.id }}" style="display:none" class="province-wrapper">
                <label class="hidden" for="AddressProvince_{{ address.id }}">{{ 'account.customer.province' | t }}</label>
                <div class="select-wrapper"><select id="AddressProvince_{{ address.id }}" name="address[province]" data-default="{{ form.province }}"></select></div>
              </div>
            </div>
            
            <div>
              <label class="hidden" for="AddressZip_{{ address.id }}">{{ 'account.customer.zip' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.zip' | t }}" id="AddressZip_{{ address.id }}" name="address[zip]" value="{{ address.zip }}" autocapitalize="characters">
            </div>

            <div>
              <label class="hidden" for="AddressPhone_{{ address.id }}">{{ 'account.customer.phone' | t }}</label>
              <input placeholder="{{ 'account.customer.phone' | t }}" type="tel" id="AddressPhone_{{ address.id }}" name="address[phone]" value="{{ address.phone }}">
            </div>

            <p>
              {{ form.set_as_default_checkbox }}
              <label class="hidden" for="address_default_address_{{ address.id }}">{{ 'account.address.set_default' | t }}</label>
            </p>

            <button type="submit" class="Btn Btn--big">{{ 'account.address.save' | t }}</button>

            {% if form.errors %}
              <script>
                $(document).ready(function() {
                  var AddressPopup_{{ address.id }} = new Focus('#PopupAddress_{{ address.id }}');
                  AddressPopup_{{ address.id }}.show();
                });
              </script>
            {% endif %}
          {% endform %}

          <a href="#close" class="popup-close" data-close data-target="#PopupAddress_{{ address.id }}" aria-label="{{ 'common.close' | t }}"><i class="material-icons">close</i></a>
        </div>
      </div>
    </div>

    <div id="PopupAddressDelete_{{ address.id }}" class="AddressPopup popup overlay">
      <div class="popup-inner">
        <div class="popup-content">
          <h4 class="AccountHeading AccountHeading--popup">{{ 'account.address.delete' | t }}</h4>
          <hr>

          <p>{{ 'account.address.delete_confirm' | t }}</p>
          <a href="#close" class="popup-close" data-close data-target="#PopupAddressDelete_{{ address.id }}" aria-label="{{ 'common.close' | t }}"><i class="material-icons">close</i></a>
          <p><a href="#" class="Btn Btn--big" onclick="Shopify.CustomerAddress.destroy({{ address.id }});return false">{{ 'account.address.delete' | t }}</a></p>
        </div>
      </div>
    </div>

  {% else %}

    <div id="PopupAddress" class="AddressPopup popup overlay">
      <div class="popup-inner">
        <div class="popup-content">
          {% form 'customer_address', customer.new_address %}
            <h4 class="AccountHeading AccountHeading--popup">{{ 'account.address.add' | t }}</h4>
            <hr>
            {{ form.errors | default_errors }}
            <div class="AddressPopup__two-column">
              <label class="hidden" for="AddressFirstNameNew">{{ 'account.customer.first_name' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.first_name' | t }}" id="AddressFirstNameNew" name="address[first_name]" value="{{ form.first_name }}" autocapitalize="words">

              <label class="hidden" for="AddressLastNameNew">{{ 'account.customer.last_name' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.last_name' | t }}" id="AddressLastNameNew" name="address[last_name]" value="{{ form.last_name }}" autocapitalize="words">
            </div>

            <div>
              <label class="hidden" for="AddressCompanyNew">{{ 'account.customer.company' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.company' | t }}" id="AddressCompanyNew" name="address[company]" value="{{ address.company }}" autocapitalize="words">
            </div>

            <div>
              <label class="hidden" for="AddressAddress1New">{{ 'account.customer.address1' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.address1' | t }}" id="AddressAddress1New" name="address[address1]" value="{{ address.address1 }}" autocapitalize="words">
            </div>

            <div>
              <label class="hidden" for="AddressAddress2New">{{ 'account.customer.address2' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.address2' | t }}" id="AddressAddress2New" name="address[address2]" value="{{ address.address2 }}" autocapitalize="words">
            </div>


            <div class="AddressPopup__two-column">
              <label class="hidden" for="AddressCityNew">{{ 'account.customer.city' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.city' | t }}" id="AddressCityNew" name="address[city]" value="{{ address.city }}" autocapitalize="words">

              <label class="hidden" for="AddressCountryNew">{{ 'account.customer.country' | t }}</label>
              <div class="hidden select-wrapper"><select id="AddressCountryNew" name="address[country]" data-default="{{ address.country }}">{{ country_option_tags }}</select></div>

              <div id="AddressProvinceContainerNew" style="display:none" class="province-wrapper">
                <label class="hidden" for="AddressProvinceNew">{{ 'account.customer.province' | t }}</label>
                <div class="select-wrapper"><select id="AddressProvinceNew" name="address[province]" data-default="{{ address.province }}"></select></div>
              </div>
            </div>

            <div>
              <label class="hidden" for="AddressZipNew">{{ 'account.customer.zip' | t }}</label>
              <input type="text" placeholder="{{ 'account.customer.zip' | t }}" id="AddressZipNew" name="address[zip]" value="{{ address.zip }}" autocapitalize="characters">
            </div>            

            <div>
              <label class="hidden" for="AddressPhoneNew">{{ 'account.customer.phone' | t }}</label>
              <input placeholder="{{ 'account.customer.phone' | t }}" type="tel" id="AddressPhoneNew" name="address[phone]" value="{{ address.phone }}">
            </div>

            <p>
              {{ form.set_as_default_checkbox }}
              <label class="hidden" for="address_default_address_new">{{ 'account.address.set_default' | t }}</label>
            </p>

            <button type="submit" class="Btn Btn--big">{{ 'account.address.save' | t }}</button>

            {% if form.errors %}
              <script>
                $(document).ready(function() {
                  var AddressPopup = new Focus('#PopupAddress');
                  AddressPopup.show();
                });
              </script>
            {% endif %}
          {% endform %}

          <a href="#close" class="popup-close" data-close data-target="#PopupAddress" aria-label="{{ 'common.close' | t }}"><i class="material-icons">close</i></a>
        </div>
      </div>
    </div>

{% endcase %}
